{% extends 'map/index.html.twig' %}

{% block global %}
<div>
    <ul class="list-group">
        <h4>Liste des calques existants</h4>
        <div class="listClq">
        {% for calque in calques %}
            {#<span class="me-2">{{ calque.nom }}</span>#}
            <div class="divClqEntier">
                <div class="divClqNom">
                    <input id="{{ calque.nom }}" type="text" value="{{ calque.nom }}" disabled/>
                </div>
                <div>
                    <a href="" id="{{ calque.nom }}M" class="am">
                        <span title="Modifier un élement" class="material-icons">edit</span>
                    </a>
                    <a href="{{ path('del_calque', { 'id':calque.id }) }}" id="{{ calque.nom }}S" onclick="return confirm('Etes vous certains de vouloir supprimer ce calque ?')">
                        <span title="Supprimer un élement" class="material-icons">delete</span>
                    </a>
                </div>
            </div>
        {% endfor %}
        </div>
    </ul>
</div>
{#
<div>
    <h1>Créer un nouvel élément sur un calque existant :</h1>
    {{ render(controller('App\\Controller\\')) }}
    {{ form(choiceCalqueForm) }}

    <form action="{{ path('select_calque') }}" method="post">
        <select class="form-select" aria-label="Default select example">
            {% for calque in calques %}
                <option value="{{ calque.nom|lower }}">{{ calque.nom }}</option>
            {% endfor %}
        </select>
        <button type="submit" class="btn btn-primary">Sélectionner le calque</button>
    </form>
</div>
#}
{% endblock %}